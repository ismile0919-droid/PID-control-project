K_p값(상수)를 결정하는 것이 P제어의 목표이다. 이는 실험을 통해 확인해야한다.

u = K_p * Error

값이 너무 작으면 SV에 도달하지 못하고, 값이 너무 크면 헌팅이 크게 발생하고 진동이 심해짐. 따라서 이 K값을 적절히 조절하여 튜닝하는 것이 필요.

-응답속도(모터 회전 속도)
-오버슈트
-진동
-정상상태오차(최종 얼마나 벗어나는지)
-안정성
